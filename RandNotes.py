# to navigate to new directory in cmd: cd C:\Folder\folder1\etc

# to navigate to new directory in bash: /c/Users/Harb/etc
#   don't use "/" for relative path

#clone a repository from github by copying the clone link and entering into the cmd: git clone [url]
#   to push a file to github, navigate to specified directory, and enter: git add [filename]
#   then: git commit -m "comment describing the change"
#   then: git push origin main /or/ git push origin HEAD:main
